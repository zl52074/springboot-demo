server:
  port: 8080
spring:
  application:
    name: demo
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://mysql-service.mysql:3306/test
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
